# Python 3.12 is the tested version in the current `enco` environment.
#
# Install:
#   pip install -r requirements.txt
#
# Notes:
# - For GPU PyTorch, install the correct torch build for your CUDA version.
# - Graphviz layout in plots requires the system `graphviz` package (the `dot` binary).

numpy
scipy
pandas
matplotlib
networkx
pydot
tqdm
scikit-learn

# ENCO
torch

# LLM queries
openai
tiktoken
google-genai

# Optional: local/HF models via `experiments/query_api.py --provider hf`
transformers
accelerate
sentencepiece

# Optional: classical baselines used by the benchmark builder
pgmpy
causal-learn
